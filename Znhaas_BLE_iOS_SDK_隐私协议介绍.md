# Znhaas BLE iOS SDK 隐私协议介绍

## 1. 文档用途

本文档用于说明 Znhaas BLE iOS SDK 在接入和运行过程中涉及的数据处理范围、系统权限用途及接入方责任，可作为内部评审和宿主应用隐私披露参考材料。

## 2. SDK 基本说明

Znhaas BLE iOS SDK 用于连接和控制 znhaas 安全帽 BLE 设备，主要能力包括：

- 蓝牙状态监听
- 蓝牙设备扫描
- 蓝牙设备连接与断开
- 设备通知数据监听
- 录制控制指令发送

## 3. SDK 处理的权限与用途

### 3.1 蓝牙权限

SDK 使用 iOS `CoreBluetooth` 能力完成设备扫描、连接、控制与回传监听。宿主 App 需要在 `Info.plist` 中配置蓝牙用途说明，例如：

- `NSBluetoothAlwaysUsageDescription`
- `NSBluetoothPeripheralUsageDescription`

用途说明：

- 扫描名称前缀为 `znhaas` 的安全帽 BLE 设备
- 连接指定安全帽设备
- 向设备发送录制控制命令
- 接收设备通过 Notify 返回的状态信息

### 3.2 不涉及的权限

当前 SDK 不主动申请或使用以下权限：

- 定位权限
- 通讯录权限
- 相册权限
- 麦克风权限
- 相机权限
- 日历权限

## 4. SDK 处理的数据类型

SDK 运行过程中可能处理以下数据：

- 蓝牙设备广播名称
- 蓝牙设备 `UUID identifier`
- 蓝牙信号强度 `RSSI`
- BLE Service / Characteristic UUID
- 业务发送的录制控制命令
- 设备回传的 BLE Notify 数据

## 5. 数据处理边界

### 5.1 SDK 不主动上传数据

当前 SDK 仅在本地完成蓝牙扫描、连接、写入和通知监听，不内置网络请求能力，不主动向任何服务器上传用户信息、设备信息或蓝牙数据。

### 5.2 SDK 不主动采集个人身份信息

当前 SDK 不主动采集以下个人信息：

- 姓名
- 手机号
- 邮箱地址
- 身份证号
- 通讯录信息
- 人脸信息
- 精确地理位置

### 5.3 业务数据由接入方决定

如果宿主应用将 `requestId`、设备标识、用户账号、日志信息或设备回传内容上传至自有服务端，则该部分数据处理行为由宿主应用自行决定并承担合规责任。

## 6. 第三方依赖情况

当前 SDK 基于 Apple 原生 `CoreBluetooth` 框架实现，不依赖第三方 BLE SDK。

## 7. 接入方责任

接入方在集成本 SDK 时，建议履行以下责任：

- 在隐私政策中明确说明蓝牙权限用途
- 在权限申请弹窗文案中说明扫描与连接 znhaas 设备的业务目的
- 对设备控制日志、设备回传日志做好访问控制和存储保护
- 如将设备数据上传服务器，需自行补充相应的隐私披露与合规说明

## 8. 安全建议

- 建议仅在必要页面和必要时机启动蓝牙扫描
- 建议在不需要时及时停止扫描和断开连接
- 建议对业务侧日志中的设备标识、指令内容进行脱敏或分级管理
- 建议对 BLE 回传数据做好最小化存储

## 9. 结论

Znhaas BLE iOS SDK 的职责范围聚焦于本地蓝牙连接与控制，不主动采集与上传个人信息。宿主应用如需结合业务账号、日志平台、远程接口进一步处理数据，应由接入方自行承担相应的隐私合规义务。
